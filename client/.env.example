# Production backend base API endpoint (leave blank to fallback to Vite proxy '/api')
VITE_API_BASE_URL=

# Socket server endpoint (defaults to http://localhost:5000 in development)
VITE_SOCKET_URL=http://localhost:5000
